# jsdf-chiiki-events — はじめてガイド

自衛隊の地方協力本部（各都道府県の自衛隊の窓口）が開催するイベントを
自動で集めてスマホアプリのように表示するウェブサービスです。

---

## このサービスが何をしているか（全体像）

```
① 自動収集（スクレイピング）
   └─ 1日3回、47都道府県の自衛隊ホームページを自動で見て
      イベント情報を集める

② データ保存
   └─ 集めた情報を public/data/events.json というファイルに書き出す

③ ウェブサイト公開
   └─ Vercel というサービスに自動アップロードして
      誰でもスマホ・PCから見られるようにする

④ プッシュ通知
   └─ 新しいイベントが増えたときにスマホへお知らせを送る
```

---

## フォルダ構成（何がどこにあるか）

```
jsdf-chiiki-events/
│
├── src/                    ← ウェブサイトの見た目を作るファイル群
│   ├── components/         ← 画面のパーツ（ボタン・地図・一覧など）
│   │   ├── HomeScreen.jsx  ← トップ画面（日本地図が表示される）
│   │   ├── JapanMap.jsx    ← 日本地図のパーツ
│   │   ├── ListScreen.jsx  ← イベント一覧画面
│   │   └── Shared.jsx      ← 複数の画面で使い回すパーツ
│   ├── data/
│   │   ├── regionMap.js    ← 都道府県と地域（関東・近畿など）の対応表
│   │   └── prefectureShapes.js ← 地図上の都道府県の形データ
│   └── hooks/
│       └── useEvents.js    ← イベントデータを取ってくる処理
│
├── scraper/                ← 自動収集プログラム
│   ├── index.js            ← 「全都道府県を順番に処理する」メインプログラム
│   └── parsers/            ← 都道府県ごとのデータ取得プログラム（47ファイル）
│       └── utils.js        ← 全都道府県で共通して使う便利な関数
│
├── public/
│   ├── data/events.json    ← 集めたイベントデータ（このファイルをサイトが読む）
│   ├── events.html         ← Google検索向けの全イベント一覧ページ
│   ├── events/             ← 都道府県別の検索向けページ（例: kagawa.html）
│   ├── sitemap.xml         ← Google に「このサイトのページ一覧」を伝えるファイル
│   └── robots.txt          ← Google に「このサイトは全部見ていいよ」と伝えるファイル
│
├── scripts/                ← ビルド補助スクリプト
│   ├── generate-events-html.mjs ← 検索エンジン向けのページを自動生成する
│   └── generate-icons.mjs       ← アプリアイコンを生成する
│
└── .github/workflows/
    ├── scrape.yml          ← 「毎日4回自動でデータ収集する」スケジュール設定
    └── deploy.yml          ← 「見た目ファイルを変更したら自動で公開する」設定
```

**よく触るファイル:**
- イベントの見た目を変えたい → `src/components/` の中
- データ収集の動きを変えたい → `scraper/parsers/<都道府県名>.js`
- スケジュールを変えたい → `.github/workflows/scrape.yml`

---

## よく使うコマンド

ターミナル（コマンドプロンプト）でプロジェクトのフォルダを開いて実行します。

```bash
# 自分のPCで動作確認する（開発用）
npm run dev
# → ブラウザで http://localhost:5173 を開くと確認できる

# 公開用ファイルを作る（本番環境向けのビルド）
npm run build

# データ収集プログラムを手動で動かす
# ※ GEMINI_API_KEY という設定が必要（詳細は下記）
cd scraper && node index.js
```

---

## 外部サービスとの連携

| サービス名 | 役割 | 設定が必要なキー |
|-----------|------|----------------|
| **Vercel** | ウェブサイトをインターネットに公開する | `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` |
| **GitHub Actions** | 自動でデータ収集 & 公開を実行する仕組み | （リポジトリのシークレットに設定） |
| **Gemini API** | ポスター画像からイベント情報を読み取るAI | `GEMINI_API_KEY` |
| **ntfy.sh** | スマホへプッシュ通知を送る | `NTFY_ADMIN_TOPIC` |

> **シークレット** = 他人に見せてはいけないパスワードのような情報。
> GitHubの「Settings → Secrets」に登録することで、自動処理の中だけで使われる。

### 自動実行スケジュール
毎日 **8:00 / 12:00 / 18:00**（日本時間）に自動でデータ収集 → 変化があれば公開。

---

## データ収集の仕組み（スクレイパー）

1. **各都道府県のホームページを開く**（ブラウザを自動操作）
2. **イベント情報を探す**（ページの文章を解析する）
3. **画像がある場合はAIで読み取る**（Gemini APIでポスターの文字を抽出）
4. **QRコードがある場合はURLも取得**（画像内のQRコードからリンクを読む）
5. **結果を `events.json` に書き出す**

収集するイベントの種類（カテゴリ）:
`説明会` / `採用イベント` / `一般公開` / `艦艇公開` / `体験` / `演奏会` / `記念行事` / `広報活動` / `地域参加`

---

## ウェブサイトの見た目について

- **表示地域**: 8つの地域（北海道・東北・関東・中部・近畿・中国・四国・九州）に分けて表示
- **ダーク/ライトモード**: スマホの設定に合わせて自動で切り替わる
- **プッシュ通知チャンネル**: `jsdf-chiiki-events-7928`（ntfy アプリで購読）

---

## よくある作業の手順

### 新しい都道府県のデータ収集を追加したい
1. `scraper/parsers/` の中に `<都道府県名>.js` を作る（他の県のファイルをコピーして改造）
2. `scraper/index.js` にそのファイルを読み込む設定を追加する
3. `src/data/regionMap.js` に都道府県名と地域の対応を追加する

### イベントの分類ルールを変えたい
- `scraper/parsers/utils.js` を開いて `guessCategory()` という部分を編集する
- すでに集まっているデータ（`events.json`）は手動で直す必要がある場合あり

### 手動でデータ収集を実行したい
GitHub のページ → `Actions` タブ → 「スクレイピング & データ更新」→ `Run workflow` ボタン

---

## Google検索対策（SEO）の状況

### 対応済みの内容

| 対応内容 | 効果 |
|---------|------|
| **Google Search Console 登録済み** | Googleに「このサイトの管理者です」と証明できた |
| **サイトマップ送信済み** | Googleが全ページを効率よく発見できる |
| **都道府県別ページ自動生成** | 「香川 自衛隊 イベント」などの検索で引っかかりやすくなる |
| **イベント構造化データ（JSON-LD）** | Googleが日付・会場を「イベント」として認識できる |
| **canonical・Twitter Card** | SNSでのシェア見た目改善、重複ページ問題を防ぐ |

### 今後の流れ（自動）
- スクレイピングのたびに `events/` 内の都道府県別ページと `sitemap.xml` が自動更新される
- 数週間〜1ヶ月後に検索結果への反映が期待できる

---

## Claude（AI）を使った作業支援ツール

このプロジェクトでは Claude AI に以下の機能が使える:

| 機能 | 使い方 |
|------|--------|
| **ウェブページを読む** | 地本サイトのページを開いて、どんな構造か調べる |
| **GitHub を操作する** | 自動処理の実行状況確認、エラー通知の確認 |
| **ブラウザを自動操作** | 実際にブラウザを動かして動的なページを調査する |

---

## 用語集（わからない言葉が出たら）

| 用語 | 意味 |
|------|------|
| **スクレイピング** | ウェブページを自動で開いて情報を収集すること |
| **パーサー** | 収集したデータを整理・解析するプログラム |
| **Vercel** | ウェブサイトを簡単に公開できるクラウドサービス |
| **GitHub Actions** | GitHubのリポジトリ上で処理を自動化する仕組み |
| **npm run dev** | 開発用の確認環境をPCで起動するコマンド |
| **npm run build** | 公開用ファイルを生成するコマンド |
| **JSON** | データを保存するためのテキスト形式（`{"キー": "値"}` の形） |
| **PWA** | スマホのホーム画面に追加してアプリのように使えるウェブサイト |
| **OCR** | 画像の中にある文字をAIが読み取ること |
| **プッシュ通知** | スマホに届くお知らせ（LINEの通知のようなもの） |
| **シークレット** | パスワードのような秘密の設定値。GitHubに安全に保管する |
| **ビルド** | 公開用のファイルを生成すること |
| **リポジトリ** | プロジェクトのファイルをGitHubで管理する場所 |
