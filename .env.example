# プッシュ通知トピック名（ntfy.sh）
# 実際の値は .env.local に設定し、Vercel 環境変数にも同じ値を登録する
VITE_NTFY_TOPIC=your-ntfy-topic-here

# OCR fallback keys used by scraper/GitHub Actions.
# RapidOCR is local and does not need an API key.
GROQ_API_KEY=
GEMINI_API_KEY=
MISTRAL_API_KEY=
OCR_SPACE_API_KEY=
OCR_SPACE_ENGINE=1
